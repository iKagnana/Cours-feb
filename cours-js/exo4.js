const sortedCharacterArray = (characterArray) => {
    return characterArray.sort()
}

const characterArray = ["aller", "kangourou", "zèbre", "histoire", "eau", "patate", "algèbre", "il"]

const result = sortedCharacterArray(characterArray)
console.log(result)